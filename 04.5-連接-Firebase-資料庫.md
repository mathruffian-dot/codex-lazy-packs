# Codex 懶人包 #04.5：連接 Firebase 資料庫

> 版本：v0.4（Codex 版，已在 Codex Desktop 實測）
> 更新日期：2026-06-09

---

## 這個懶人包會幫你做什麼？

讓 Codex CLI 直接操控 Firebase Cloud Firestore：
- 自然語言建集合、CRUD（透過 Firebase MCP 工具）
- 即時更新（學生輸入後展示頁面馬上顯示）
- **千人研習也撐得住**（並發 100 萬）
- **永遠不會閒置暫停**

> 💡 對老師而言這是比 Supabase 更友善的預設選擇。Supabase 適合重度 SQL 統計分析，Firebase 對絕大多數教學工具限制更少。

### Firebase vs Supabase

| 比較項目 | Supabase（#04） | Firebase（本懶人包） |
|---|---|---|
| 資料庫類型 | SQL | NoSQL |
| 免費專案數 | 2 個 | 無限 |
| 閒置暫停 | 一週沒用會暫停 | **不會暫停** |
| 並發連線（免費） | 200 | **100 萬** |
| 即時更新 | 有 | 更強（onSnapshot） |
| MCP 工具 | 完整 | **完整**（list/query/add/update/delete + auth + messaging + storage） |

---

## 先備條件

- [ ] Codex CLI 已安裝
- [ ] Google 帳號
- [ ] 電腦有網路連線

---

## 階段一：建立 Firebase 專案

### 步驟零：環境檢查

1. 作業系統 / 網路 / `node --version`（沒裝就裝）
2. Windows PowerShell 建議用 `npx.cmd --version`，避免 PowerShell 執行原則擋到 `npx.ps1`

---

### 步驟一：建立 Firebase 專案

> 🖐️ 到 https://console.firebase.google.com → 建立專案：
> - 專案名稱：`my-teaching-tools`
> - Google Analytics 可不啟用
> - 等建立完成

---

### 步驟二：啟用 Cloud Firestore

> 🖐️ Console → Firestore Database → 建立資料庫：
> - Standard 版
> - 位置：`asia-east1 (Taiwan)` 或 `asia-northeast1 (Tokyo)`
> - **安全性規則：「以正式版模式啟動」**（不要選測試模式！測試模式 30 天會過期）
> - 建立

---

### 步驟二之二：設定 Firestore 安全規則（自動化）

請 Codex 在使用者的專案資料夾建立三個檔案：

**1. `firestore.rules`**：
```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /wordcloud_words/{document} {
      // 測試用公開白名單：只適合課堂 demo，不可直接用在正式學生資料或長期公開服務。
      allow read, write: if true;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
```

**2. `firebase.json`**：
```json
{
  "firestore": {
    "rules": "firestore.rules"
  }
}
```

**3. `.firebaserc`**：
```json
{
  "projects": {
    "default": "[使用者的 Firebase 專案 ID]"
  }
}
```

部署規則：
```powershell
npx.cmd -y firebase-tools@latest deploy --only firestore:rules
```

成功會看到 `Deploy complete!`。

> 💡 之後新工具：測試階段可在 `firestore.rules` 加 `match /votes/{document} { allow read, write: if true; }` → 對 Codex 說「部署 Firestore 規則」。正式服務請改成登入、班級碼或教師管理條件，不要沿用公開寫入規則。

---

## 階段二：連接 MCP

### 步驟三：把 Firebase 註冊為 Codex 的 MCP server

> ✅ 三種 Codex 共用 `~/.codex/config.toml`。

**方法 A：Codex Desktop GUI（推薦）**

1. Desktop → 設定 → **Integrations & MCP** → **Add server**
2. 填：
   - Name：`firebase`
   - Command：Windows 建議填 `npx.cmd`；macOS / Linux 填 `npx`
   - Args：`-y`、`firebase-tools@latest`、`mcp`
3. 儲存

**方法 B：手動編輯 `~/.codex/config.toml`**

```toml
[mcp_servers.firebase]
command = "npx.cmd"
args = ["-y", "firebase-tools@latest", "mcp"]
startup_timeout_sec = 60
tool_timeout_sec = 120
```

> Windows 建議用 `npx.cmd`，比較不會踩到 PowerShell `.ps1` 執行原則問題。macOS / Linux 可用 `command = "npx"`。

**方法 C：CLI**

```bash
codex mcp add firebase -- npx -y firebase-tools@latest mcp
```

> 2026-04-29 實測：本機 Codex Desktop 可用「方法 B」成功載入。若 `codex` CLI 在 Windows 顯示 `Access is denied`，不要卡在 CLI，改用 GUI 或手動改 `config.toml`。

---

### 步驟四：登入 Firebase CLI

```powershell
npx.cmd -y firebase-tools@latest login
```

> 🖐️ 瀏覽器會開 Google 登入頁，授權即可。

驗證：
```powershell
npx.cmd -y firebase-tools@latest projects:list
```

> ⚠️ `firebase login` 必須在**互動式終端**執行。如果在 Codex 對話中卡住，請使用者打開 cmd / PowerShell 手動跑一次。

---

### 步驟五：重啟 Codex 並驗證

> 🖐️ **Desktop**：完全結束 app 重開；**IDE**：Reload Window；**CLI**：`exit` 後重開。

對 Codex 說：

```text
幫我列出 Firebase 專案，然後列出 Firestore 集合，接著新增一筆 test_collection 測試資料，再讀回來，最後刪掉。
```

如果 Firebase MCP 已成功載入，Codex 會看到類似工具：
- `firebase_list_projects`
- `firestore_list_collections`
- `firestore_add_document`
- `firestore_get_document`
- `firestore_delete_document`

---

### 步驟六：功能測試

1. 列出 Firebase 專案
2. 列出目前 Firestore 集合
3. 在 Firestore 建集合 `test_collection`，新增一筆 `message`「Firebase 連接測試成功」
4. 查 `test_collection` 確認能讀
5. 刪除測試文件
6. 再讀一次，確認文件不存在
7. ✅ 「連接測試成功！」

### 2026-04-29 Codex Desktop 實測結果

測試環境：
- Node：`v24.14.0`
- npx：`11.9.0`
- Firebase CLI：可列出專案
- MCP 設定位置：`C:\Users\mathr\.codex\config.toml`

成功列出的 Firebase 專案：
- `my-teaching-tools`
- `teacherstudy-109ef`

以 `teacherstudy-109ef` 測試 Firestore：
- 成功列出集合：`game_scores`、`taipei_principal`、`wordcloud_words`
- 成功新增：`test_collection/codex_mcp_test_20260429`
- 成功讀回欄位：
  - `message`：`Firebase 連接測試成功`
  - `source`：`Codex Firebase MCP test`
- 成功刪除
- 刪除後再次讀取，回報文件不存在

### 2026-05-01 Codex Desktop 實測結果

測試環境：
- 專案資料夾：Google Drive 同步資料夾 `G:\我的雲端硬碟\2026database`
- Firebase 專案：`teacherstudy-109ef`
- Node：`v24.14.0`
- npx：`11.9.0`
- 執行方式：Windows PowerShell 改用 `npx.cmd`，不要直接用 `npx`

本次確認成功：
- `npx.cmd -y firebase-tools@latest projects:list` 可列出 `my-teaching-tools` 與 `teacherstudy-109ef`
- `npx.cmd -y firebase-tools@latest deploy --only firestore:rules` 成功部署，看到 `Deploy complete!`
- Firebase MCP server 可啟動，版本 `firebase 0.3.0`
- MCP 工具清單包含 `firestore_add_document`、`firestore_get_document`、`firestore_list_documents`、`firestore_delete_document`
- 成功新增測試文件：`test_collection/codex_mcp_read_test_20260501`
- 成功讀回欄位：
  - `message`：`Codex Firebase MCP read test OK`
  - `source`：`codex-db-read-test`
  - `testDate`：`2026-05-01`
- 成功列出集合文件，確認剛新增的文件存在
- 成功刪除測試文件

本次踩坑：
- PowerShell 直接跑 `npx --version` 會因為 `npx.ps1` 被執行原則擋住而失敗；改用 `npx.cmd --version`。
- Codex 讀 Google Drive 工作資料夾時可能遇到 sandbox 權限，要授權後再執行。
- `firestore_query_collection` 曾回 `read_time cannot be in the future`；這看起來是 MCP 查詢工具的時間參數問題。若只是要驗證資料庫可讀寫，可先用 `firestore_get_document` 與 `firestore_list_documents` 確認。
- 用腳本處理 GitHub 中文檔名時，要小心 PowerShell 管線或 Node stdin 把中文轉成問號；可改用 URL encoded path 或純 PowerShell Unicode 流程。

### 2026-06-09 Codex Desktop macOS 實測結果

測試環境：
- 專案資料夾：Google Drive 同步資料夾 `/Users/.../GoogleDrive.../2026Database`
- Firebase 專案：`teacherstudy-dd1d2`
- Firebase CLI：standalone binary `15.19.1`
- 登入帳號：`tzengbh@gmail.com`
- Firestore 測試集合：`wordcloud_words`

本次確認成功：
- `firebase projects:list` 可列出 `teacherstudy-dd1d2`，並確認它是 current project。
- `firebase deploy --only firestore:rules` 成功部署，看到 `Deploy complete!`。
- Firebase Console 專案若沒有 Web App，可用 `firebase apps:create WEB teacherstudy-web --project teacherstudy-dd1d2` 建立，再用 `firebase apps:sdkconfig WEB [App ID] --project teacherstudy-dd1d2` 取回前端設定。
- 前端 Firebase config 必須和 `.firebaserc` 指向同一個 project；否則 rules 部署到新專案，但網頁仍會連到舊資料庫。
- 透過 Firestore REST API 測試 `wordcloud_words`：
  - 新增文件成功
  - 讀取單筆文件成功
  - 更新欄位成功
  - 列出集合文件成功
  - 刪除文件成功
  - 刪除後再讀取，回 `404 NOT_FOUND`

本次踩坑：
- macOS 的 Codex Desktop 內建 Node 不一定有 `npm` / `npx`；終端可能出現 `npx: command not found`，但 `node --version` 仍正常。
- 如果沒有 `npx`，可改用官方 Firebase CLI standalone binary：`https://firebase.tools/bin/macos/latest`。下載後先放在暫存路徑，例如 `/private/tmp/firebase_bin`，再 `chmod +x` 執行。
- Firebase standalone CLI 第一次啟動會寫入 `~/.cache/firebase`。在 Codex 沙盒中可能遇到 `EPERM: operation not permitted, mkdir '/Users/.../.cache/firebase'`，需要授權 CLI 建立官方快取。
- `firebase login --no-localhost` 可能要求貼 authorization code；若登入頁沒有顯示 code，改跑一般 `firebase login`，讓它用本機 `localhost` callback 完成授權。
- Codex 工具清單裡若暫時沒有 Firebase MCP 工具，仍可先用 Firebase CLI + Firestore REST API 驗證資料庫可讀寫，避免卡在 MCP 載入問題。
- 公開前端的 Firebase config 可以放在 HTML 裡，但它只負責連線；真正的讀寫權限仍由 Firestore rules 控制。
---

## 完成！這樣用

| 你說的話 | Codex + Firebase 會做的事 |
|----------|---------------------------|
| 「做一個即時文字雲，連接 Firebase」 | 產前端 + 連 Firestore + 即時更新 |
| 「做課堂投票工具」 | 產網頁 + 改 firestore.rules + 自動部署 |
| 「部署 Firestore 規則」 | `firebase deploy --only firestore:rules` |
| 「查 wordcloud_words 最熱門 5 個關鍵字」 | 直接呼叫 `firestore_query_collection` |
| 「刪掉所有測試資料」 | `firestore_delete_document` |

---

## 如果失敗

對 Codex 說：「Firebase 懶人包失敗，幫我檢查重新處理。」

完全重置：
- **Desktop**：設定 → Integrations & MCP → 刪 `firebase`
- **手動**：編輯 `~/.codex/config.toml` 刪 `[mcp_servers.firebase]` 段
- **CLI**：`codex mcp remove firebase`

重登 Firebase：
```powershell
npx.cmd -y firebase-tools@latest logout
npx.cmd -y firebase-tools@latest login
```

---

## 常見問題

| 問題 | 可能原因 | 解法 |
|------|----------|------|
| `npx: command not found` | Node.js / npm 沒裝好，或終端 PATH 沒帶到 | 確認 Node.js 已裝，重啟終端機 |
| macOS Codex Desktop 有 `node` 但沒有 `npm` / `npx` | Codex 內建 runtime 不一定包含 npm/npx | 可下載 Firebase CLI standalone binary，或在本機正常安裝 Node.js/npm 後重開終端 |
| Firebase standalone CLI 回 `firepit-log.txt`，內容含 `EPERM ... ~/.cache/firebase` | CLI 第一次啟動需要建立 `~/.cache/firebase` | 授權 Codex 讓 Firebase CLI 寫入官方快取後重跑 |
| PowerShell 顯示 `npx.ps1 cannot be loaded because running scripts is disabled` | Windows 執行原則擋到 `npx.ps1` | 改用 `npx.cmd --version`、`npx.cmd -y firebase-tools@latest ...` |
| Codex 讀 Google Drive 專案資料夾出現 sandbox 權限錯誤 | Codex 需要使用者授權讀寫該路徑 | 同意授權後重跑；不要先判定檔案壞掉 |
| 查詢失敗 | Firebase CLI 尚未登入、登入過期，或 MCP 尚未重新載入 | 確認 `firebase login` 已成功，必要時重啟 Codex |
| `firebase login --no-localhost` 登入後沒有 authorization code | no-localhost 流程沒有成功產生可貼回終端的 code | 改用一般 `firebase login`，讓瀏覽器透過本機 `localhost` callback 自動完成登入 |
| `firestore_query_collection` 回 `read_time cannot be in the future` | MCP 查詢工具可能送出未來時間參數 | 先用 `firestore_get_document` 或 `firestore_list_documents` 驗證可讀取，必要時重啟 MCP 後再查 |
| 看不到 Firestore 資料 | Console 尚未啟用 Firestore，或前端連到不同 project | 確認 Console 已啟用 Firestore，並核對 `projectId` |
| rules 已部署成功，但前端網頁仍連不到資料 | `.firebaserc` 和 HTML 裡的 Firebase config 指向不同專案 | 檢查 HTML 裡的 `firebaseConfig.projectId` 是否和 `.firebaserc` 的 default project 相同 |
| Firebase 專案裡沒有 Web App，找不到前端 config | 專案只有 Firestore，尚未建立 Web App | 用 `firebase apps:create WEB [app-name] --project [project-id]` 建立，再用 `firebase apps:sdkconfig WEB [app-id] --project [project-id]` 取回 config |
| 安全性規則過期 | 建資料庫時用了測試模式 | 改白名單規則（步驟二之二），再部署 rules |
| `Permission denied` | rules 沒允許這個集合或操作 | 檢查 `firestore.rules` 的 match 區塊是否包含該 collection |
| `Collection id is invalid because it contains /` | collection path 多打了尾端斜線 | `collection_path` 別含尾巴 `/`，寫 `wordcloud_words` 不要 `wordcloud_words/` |
| `firebase login` 在對話中卡住 | 互動式登入需要瀏覽器授權 | 開 cmd/PowerShell 手動跑 `npx firebase-tools login`，或在 macOS 讓 `firebase login` 用 localhost callback |
| Codex 重開前看不到 Firebase 工具 | MCP server 只在啟動時載入 | 改完 `~/.codex/config.toml` 後，要完整重開 Codex Desktop；只在同一輪對話重新搜尋通常不會載入新 MCP server |
| Windows 跑 `npx` 出問題 | 執行到 PowerShell 的 `npx.ps1` 或 PATH 解析錯誤 | 把 MCP 設定的 `command` 改成 `npx.cmd` |
| `codex --version` 顯示 `Access is denied` | Windows CLI 權限或安裝狀態問題 | 不影響 Codex Desktop 實測。改走 GUI 或手動編輯 `~/.codex/config.toml` |
| `firestore_add_document` 說 document 格式 invalid | Firestore MCP 文件格式包錯 | 不要把 Firestore document 包成 JSON 字串；要傳 document 物件格式，例如 `fields.message.stringValue` |

---

## 免費方案（Spark）

| 項目 | 額度 |
|------|------|
| Firestore 儲存 | 1 GB |
| 讀取 | 50,000 次/天 |
| 寫入 | 20,000 次/天 |
| 專案數 | **無限** |
| 閒置暫停 | **不會** |

---

## 安全

- 不要把 Firebase Admin 憑證進公開 repo
- 前端 Firebase Config（apiKey 等）設計給前端用，可以公開
- 學生資料只存座號 + 班級代號，不存真名

---

## 相關連結

- [Firebase 官網](https://firebase.google.com)
- [Firebase MCP Server 文件](https://firebase.google.com/docs/ai-assistance/mcp-server)
- [Codex MCP 官方文件](https://developers.openai.com/codex/mcp)
